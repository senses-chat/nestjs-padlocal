{
  "name": "@senses-chat/wechat-padlocal",
  "version": "0.1.0",
  "description": "Wechat Padlocal Module",
  "author": "Simon Liang <simon@x-tech.io>",
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "prebuild": "npm run clean",
    "proto:generate": "proto-loader-gen-types --keepCase --longs=Number --defaults --oneofs --grpcLib=@grpc/grpc-js --outDir=src/generated padlocal.proto",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "format": "prettier --write",
    "lint": "eslint --fix"
  },
  "dependencies": {
    "padlocal-client-ts": "^0.4.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^8.3.0",
    "@nestjs/config": "^1.1.5",
    "@nestjs/core": "^8.3.0",
    "@nestjs/cqrs": "^8.0.2",
    "class-transformer": "^0.5.1",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.4.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.0"
  },
  "volta": {
    "node": "16.13.1",
    "yarn": "1.22.17"
  }
}
